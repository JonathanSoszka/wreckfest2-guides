{
  "name": "wreckfest-guides",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && npx wrangler pages deploy dist --project-name=wreckfest2-guides --branch=main --commit-dirty=true",
    "deploy:preview": "astro build && npx wrangler pages deploy dist --project-name=wreckfest2-guides --branch=preview --commit-dirty=true"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.3",
    "astro": "^7.1.1"
  }
}
